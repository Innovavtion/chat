{
  "name": "chat-fullstack",
  "version": "0.0.1",
  "author": "Nikita",
  "license": "ISC",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "turbo run dev",
    "dev:client": "",
    "dev:server": "",
    "build": "turbo run build",
    "start": "node apps/server/dist/main",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "only-allow": "^1.2.1",
    "turbo": "^1.13.2"
  }
}
